# Log

## 2025-04-19
### 由于Cpp初始化的值不确定，bind_pins后会自动初始化为0，always块可能会被意外调用，导致初始信号与预期不符。  

## 2025-0420
### 由于可能访问非法内存，请确保绑定的位数与信号位数匹配，否则可能会导致非法输入/输出（主要针对INPUT和OUTPUT的十六进制）
#### 已修复上述问题，重构了结构体，增加了max_byte字段，防止越界。