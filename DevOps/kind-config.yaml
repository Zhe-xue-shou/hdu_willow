# kind-config.yaml
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  - role: control-plane
    extraMounts:
      - hostPath: /home/zxs/Desktop/workbench/hdu_willow/test   # 你宿主机的目录
        containerPath: /workspace                               # 节点容器里的路径
